version: "3.8"

x-logging: &logging
  logging:
    driver: "json-file"
    options:
      max-file: "5"
      max-size: "10m"

x-restart-policy:
  restart: unless-stopped

services:
  api:
    <<: *logging

    build:
      context: ./api
      dockerfile: Dockerfile

    env_file:
    - .env

    volumes:
    - ./api:/api

    ports:
    - "8000:8000"

    networks:
    - database-access
    - ordis

  bot:
    <<: *logging

    build:
      context: ./bot
      dockerfile: Dockerfile

    environment:
      API_URL: "http://api:8000"

    env_file:
    - .env

    depends_on:
    - api

    volumes:
    - ./bot:/bot

    networks:
    - ordis

networks:
  database-access:
    name: database-access
    external: true
  ordis:
    name: ordis
    external: true
